# Claude Code — Entry Pointer

This is a MAWA project. The canonical, tool-neutral operating instructions live in
[AGENTS.md](AGENTS.md).

**Read [AGENTS.md](AGENTS.md) first, then follow it.** Do not duplicate or override its
rules here — this file exists only so Claude Code finds the entry point. The same
instructions are used by other agents (Codex, Cursor, etc.).

Claude-specific note: `governance/SKILL.md` carries the MAWA operational playbook and
can be used as a Skill, but `AGENTS.md` remains the source of truth for the workflow.
