# Google Stitch Design Setup

Google Stitch selected as design adapter.

MAWA assumes the required design tooling/MCP is already configured externally.

MAWA does not install or configure MCP servers.

Before using this adapter, confirm:

- the tool is available in the current environment;
- any required MCP server is already configured;
- the project has access to the target design workspace/file when needed.
